services:
  - type: web
    name: solver-service
    env: python
    buildCommand: "cd solver-service && pip install -r requirements.txt"
    startCommand: "cd solver-service && uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
